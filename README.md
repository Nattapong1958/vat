# VAT Tax Filing Verification System

ระบบยืนยันการยื่นภาษี ภ.ง.ด. 90/91 สำหรับ ร.19 พัน.1

## คุณสมบัติ

### 🔐 ระบบ Login & ระบุตัวตน
- **โหมดผู้ใช้**: เข้าสู่ระบบด้วยการเลือกชื่อของตนเอง - สามารถยืนยันสถานะการยื่นภาษีของตัวเองเท่านั้น
- **โหมด Admin**: เข้าสู่ระบบด้วย PIN (1919) - สามารถจัดการข้อมูลทั้งหมดได้
- Session บันทึกใน LocalStorage (หมดอายุ 24 ชั่วโมง)

### ✅ การยืนยันตัวตน (Self-Verification)
- ระบบบันทึกว่าใครเป็นผู้ยืนยันสถานะแต่ละราย
- แสดงป้าย "ยืนยันด้วยตนเอง" หรือ "โดย Admin" พร้อมวันเวลา
- ไฮไลท์แถวของตนเองด้วยสีฟ้าอ่อน

### 📊 การจัดการข้อมูล
- **5 หน่วย**: สัญญาบัตร, ร้อย.1, ร้อย.อวบ.2, ร้อย.อวบ.3, ร้อย.บก.
- รองรับ 233+ รายการบุคลากร
- ค้นหาและกรองข้อมูลด้วยชื่อ, นามสกุล, ยศ, หรือสถานะ
- แสดงสถิติและความคืบหน้าแบบ Real-time

### 📝 Audit Log
- บันทึกทุกการเปลี่ยนแปลง (Login, Logout, เปลี่ยนสถานะ)
- ระบุผู้ดำเนินการ, เวลา, และประเภทการยืนยัน (ตนเอง/Admin)
- Export เป็น CSV ได้
- เก็บประวัติ 1,000 รายการล่าสุด

### 💾 LocalStorage Persistence
- บันทึกอัตโนมัติทุก 30 วินาที
- บันทึกเมื่อปิดหน้า, เปลี่ยน Tab, หรือ Offline
- Sync ข้อมูลระหว่าง Tab (Cross-tab synchronization)
- Factory Reset สำหรับ Admin

### 🌐 Google Sheets Integration (พร้อมใช้งาน)
- พร้อมเชื่อมต่อ Google Sheets API
- Batch update สำหรับประสิทธิภาพ
- โค้ด Google Apps Script พร้อมใช้งาน (ดูไฟล์ `google-apps-script.js`)

### 🎨 Apple-Inspired Design
- ออกแบบตามหลัก Apple Design System
- รองรับ Dark mode ผ่าน CSS Variables
- Responsive design สำหรับ Mobile/Tablet/Desktop
- Smooth animations และ transitions

## โครงสร้างไฟล์

```
VAT/
├── index.html              # หน้าเว็บหลัก
├── css/
│   └── style.css          # Apple-inspired design system
├── js/
│   ├── config.js          # การตั้งค่า (API URLs, ค่าคงที่)
│   ├── data.js            # ข้อมูลกำลังพล 5 หน่วย
│   ├── api.js             # Google Sheets API + LocalStorage Manager
│   ├── auth.js            # ระบบ Login, Session, Audit Log
│   └── app.js             # Logic หลักของแอป
├── google-apps-script.js  # โค้ดสำหรับ Google Apps Script
└── README.md              # เอกสารนี้
```

## การใช้งาน

### 1. เปิดใช้งาน Local
```bash
# เปิด HTTP Server (Python)
python -m http.server 8080

# หรือ (PowerShell)
python -m http.server 8080

# เปิดเบราว์เซอร์
http://localhost:8080
```

### 2. Login
- **ผู้ใช้ทั่วไป**: เลือกชื่อของตนเองจากรายชื่อ
- **Admin**: คลิก "เข้าสู่ระบบแบบ Admin" → กรอก PIN: `1919`

### 3. ยืนยันสถานะภาษี
- ผู้ใช้: คลิก dropdown ที่แถวของตนเอง → เลือก "ยื่นภาษีแล้ว" หรือ "ยังไม่ยื่น"
- Admin: สามารถเปลี่ยนสถานะทุกคนได้

### 4. ดู Audit Log
- คลิกปุ่มนาฬิกาที่มุมขวาบน
- Export เป็น CSV ได้

### 5. Export ข้อมูล
- Export หน้าปัจจุบัน: ปุ่ม Export ด้านล่างตาราง
- Export ทั้งหมด: ปุ่ม Export ที่มุมขวาบน

## การเชื่อมต่อ Google Sheets (ถ้าต้องการ)

1. สร้าง Google Sheets ใหม่
2. ไปที่ Extensions → Apps Script
3. Copy โค้ดจากไฟล์ `google-apps-script.js` ไปวาง
4. Deploy → New deployment → Web app
5. Copy URL ที่ได้มาใส่ใน `js/config.js` → `APPS_SCRIPT_URL`

## ข้อมูลทางเทคนิค

- **Frontend**: Pure HTML/CSS/JavaScript (ไม่ใช้ Framework)
- **Storage**: LocalStorage (Primary), Google Sheets (Optional)
- **Design**: Apple Design System
- **Font**: Inter + Noto Sans Thai
- **Browser Support**: Chrome, Firefox, Safari, Edge (modern versions)

## สิทธิ์การใช้งาน

- **ผู้ใช้**: ดูทุกหน้าได้, แก้ไขเฉพาะแถวตัวเองได้
- **Admin**: เข้าถึงฟังก์ชันทั้งหมด:
  - ยื่นทั้งหมด
  - รีเซ็ตหน้านี้
  - ลบข้อมูลทั้งหมด (Factory Reset)
  - Batch Update
  - เปลี่ยนสถานะทุกคน

## License

© 2026 ร.19 พัน.1 - Tax Verification System
